解决vue历史模式(node)   下载koa2-connect-history-api-fallback

```
const {
  historyApiFallback
} = require('koa2-connect-history-api-fallback');

app.use(historyApiFallback({ whiteList: ['/public'] }));
```

